# Vercel supplies VERCEL_ENV automatically. Only production enables analytics.
# Optional kill switch: false disables analytics even in production.
PUBLIC_SIMPLE_ANALYTICS_ENABLED=true
# Evidence always forces DAB_EVIDENCE=true and never loads the external script.
